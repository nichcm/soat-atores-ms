services:
    nginx:
        image: nginx
        container_name: atores-ms-nginx-container
        ports:
            - 8080:80
        networks:
            - soat-atores-ms-net
        hostname: atores-ms-nginx-hostname
        restart: on-failure:3
        volumes:
            - ./containers/nginx/nginx.conf:/etc/nginx/nginx.conf
            - ./application:/var/www/html
        depends_on:
            microservice:
                condition: service_started

    microservice:
        container_name: atores-ms-container
        hostname: atores-ms-hostname
        build:
            context: .
            dockerfile: ./images/php/Dockerfile
        volumes:
            - ./application:/var/www/html
        restart: on-failure:3
        expose:
            - 9000
        networks:
            - soat-atores-ms-net
        depends_on:
            postgres:
                condition: service_healthy

    postgres:
        image: postgres:17.5
        container_name: atores-ms-postgres-container
        restart: always
        ports:
            - 5433:5432
        env_file:
            - ./containers/postgres/env/.env.dev
        volumes:
            - pgdata:/var/lib/postgresql/data
        networks:
            - soat-atores-ms-net
        healthcheck:
            test: ["CMD-SHELL", "pg_isready -U postgres"]
            interval: 5s
            timeout: 3s
            retries: 5
            start_period: 5s

volumes:
    pgdata:

networks:
    soat-atores-ms-net:
        name: soat-atores-ms-net
        driver: bridge
